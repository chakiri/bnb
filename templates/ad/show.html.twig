{% extends 'base.html.twig' %}

{% block title %}{{ ad.title }}{% endblock %}

{% block body %}

    <div class="cover-image" style="background-image: url({{ ad.coverImage }})"></div>

    <div class="container">
        <div class="row mt-5">
            <div class="col-md-8">
                <h1>{{ ad.title }} <a href="{{ path('ad_edit', {'slug': ad.slug}) }}"><button class="btn btn-secondary">Editer</button></a></h1>
                <p class="lead">{{ ad.introduction }}</p>
                <h4>{{ ad.rooms }} Chambres - <strong>{{ ad.price }} €</strong> par nuit</h4>
                <button type="button" class="btn btn-primary btn-lg">Réserver !</button>
                <hr>
                {# Carousel #}
                <div id="carouselExampleCaptions" class="carousel slide mb-3" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for image in ad.images %}
                            <li data-target="#carouselExampleCaptions" data-slide-to="{{ loop.index0 }}" {% if loop.first %}class="active"{% endif %}></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for image in ad.images %}
                            <div class="carousel-item {% if loop.first %}active{% endif %}">
                                <img src="{{ image.url }}" class="d-block w-100" alt="{{ image.caption }}">
                                <div class="carousel-caption d-none d-md-block">
                                    <p>{{ image.caption }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
                {# End carousel #}
                {{ ad.content|raw }}
            </div>
            <div class="col">
                <div class="row">
                    <div class="col-3">
                        <img src="http://placehold.it/64x64" alt="Avatar de yassir">
                    </div>
                    <div class="col">
                        <h3>Yassir Chakiri</h3>
                        <span class="badge badge-light">3 annonces</span>
                    </div>
                </div>
                <hr>
                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Amet animi atque autem consectetur
                    minus molestias nemo quis totam! Blanditiis distinctio dolore hic quam recusandae rem sequi
                    tempora vel vero voluptatum.</p>
                <p>Corporis est molestiae necessitatibus neque quidem, voluptas voluptatum. Blanditiis, corporis
                    esse est eum exercitationem facilis hic, impedit libero maiores natus nihil odio pariatur quod
                    ratione voluptas! Facere minima omnis quod.</p>
            </div>
        </div>
    </div>

{% endblock %}
